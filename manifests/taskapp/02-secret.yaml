apiVersion: v1
kind: Secret
metadata:
  name: taskapp-secret
  namespace: taskapp
type: Opaque
stringData:
  SECRET_KEY: "CHANGE_ME_USE_SEALED_SECRETS_OR_EXTERNAL_SECRETS"
  DATABASE_PASSWORD: "CHANGE_ME_SET_VIA_PORTAINER_OR_SEALED_SECRETS"
